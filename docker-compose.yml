version: '3.8'
services:
  movie_app:
    build: .
    container_name: mobatai_vault
    ports:
      - "5001:5000"  # ¡Cambiamos el 5000 de la izquierda por 5005!
    env_file:
      - .env
    volumes:
      - ./instance:/app/instance
    restart: unless-stopped